{
  "name": "afterwind",
  "version": "0.0.2",
  "description": "A utility package that extends Tailwind CSS' capabilities",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "start": "ts-node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Idris Abdul-Lateef",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  }
}
